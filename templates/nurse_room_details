<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Room {{ room_num }} Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 p-10 font-sans">

    <div class="max-w-4xl mx-auto">
        <div class="flex items-center justify-between mb-8">
            <h1 class="text-3xl font-bold text-gray-800">
                <i class="fas fa-clinic-medical text-blue-600 mr-2"></i> Room {{ room_num }} Details
            </h1>
            <button onclick="window.close()" class="text-gray-500 hover:text-red-500 font-bold">
                <i class="fas fa-times mr-1"></i> Close Window
            </button>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
            <div class="bg-white p-6 rounded-xl shadow-md border-l-4 {{ 'border-green-500' if doctor else 'border-red-500' }}">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-bold text-gray-400 uppercase">Assigned Doctor</p>
                        <h2 class="text-xl font-bold text-gray-800">{{ doctor.name if doctor else "No Doctor Assigned" }}</h2>
                    </div>
                    <div class="text-right">
                        {% if doctor %}
                            <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-sm font-bold">Online</span>
                        {% else %}
                            <span class="bg-red-100 text-red-700 px-3 py-1 rounded-full text-sm font-bold">Unavailable</span>
                        {% endif %}
                    </div>
                </div>
            </div>

            <div class="bg-white p-6 rounded-xl shadow-md border-l-4 border-blue-500">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-bold text-gray-400 uppercase">Total Patients</p>
                        <h2 class="text-4xl font-bold text-blue-600">{{ total_patients }}</h2>
                    </div>
                    <i class="fas fa-users text-4xl text-blue-100"></i>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl shadow-lg overflow-hidden">
            <div class="bg-gray-50 px-6 py-4 border-b border-gray-200">
                <h3 class="font-bold text-gray-700">Patient Queue for Room {{ room_num }}</h3>
            </div>
            
            {% if active_visit %}
                <table class="min-w-full text-left">
                    <thead class="bg-gray-100 text-gray-500 text-xs uppercase">
                        <tr>
                            <th class="px-6 py-3">Patient Name</th>
                            <th class="px-6 py-3">IC Number</th>
                            <th class="px-6 py-3">Symptoms</th>
                            <th class="px-6 py-3">Status</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200">
                        <tr class="hover:bg-blue-50 transition">
                            <td class="px-6 py-4 font-bold text-gray-800">{{ active_visit.patient.name }}</td>
                            <td class="px-6 py-4 text-gray-600 font-mono text-sm">{{ active_visit.patient.ic_number }}</td>
                            <td class="px-6 py-4 text-gray-600 text-sm">{{ active_visit.symptoms }}</td>
                            <td class="px-6 py-4">
                                <span class="px-3 py-1 rounded-full text-xs font-bold
                                    {{ 'bg-yellow-100 text-yellow-800' if active_visit.status == 'waiting' else 'bg-green-100 text-green-800' }}">
                                    {{ active_visit.status | capitalize }}
                                </span>
                            </td>
                        </tr>
                    </tbody>
                </table>
            {% else %}
                <div class="p-10 text-center text-gray-400">
                    <i class="fas fa-clipboard-check text-4xl mb-3"></i>
                    <p>No patients assigned to this room.</p>
                </div>
            {% endif %}
        </div>

    </div>

</body>
</html>